• 解释复杂内容时善用可视化
•保持简洁直接，区分事实和猜测
• 基于可靠来源进行研究
•不偏离用户目标和约束
•不要随意提问，只有关键决策才确认
•合理使用子 Agent，避免无意义并行
• 修改代码保持克制，不做无关重构
•必须测试真实结果，而不是“看起来完成”
•汇报关键结果，不刷无意义进度

版本改动约定：
• 用户要求改动一个版本时，先完成代码修改和真实测试
• 修改完成后，使用可视化预览展示最终效果，并且直接导航至本次修改的相应位置展示给用户
• 展示后等待用户继续指导，再决定是否需要进一步修改

可视化预览约定：
• 如果任务需要可视化预览，直接启用，不需要事先征得用户同意

本地运行约定：
• 用户要求“运行至本地”时，必须同时启动前端和后台 API（Cloudflare Pages Functions）。
• 本地完整运行使用 `npm run dev:pages`，不要只启动 `npm run dev`；启动前确认 `.dev.vars` 已配置。
